import tkinter as tk
def fahrenheit_to_celsius():
    valeur = ent_temperature.get()
    
    if valeur.replace('.', '', 1).isdigit():
        fahrenheit = float(valeur)
        celsius = (fahrenheit - 32) * 5 / 9
        celsius = round(celsius, 2)
        lbl_result.config(text=f"{celsius} °C")
    else:
        lbl_result.config(text="ERREUR")

Window= tk.Tk()
Window.title("Convertisseur de température")
Window.resizable(width=False, height=False)

frm_entry = tk.Frame(master=Window)
ent_temperature = tk.Entry(master=frm_entry, width=10)
lbl_temp = tk.Label(master=frm_entry, text="\N{DEGREE FAHRENHEIT}")


ent_temperature.grid(row=0, column=0, sticky="e") 
lbl_temp.grid(row=0, column=1, sticky="w")
frm_entry.grid(row=0, column=0, padx=10, pady=10)

btn_convert = tk.Button(master=Window, text="\N{RIGHTWARDS BLACK ARROW}", 
                        command=fahrenheit_to_celsius)
btn_convert.grid(row=0, column=1, pady=10)

lbl_result = tk.Label(Window, text="\N{DEGREE CELSIUS}")
lbl_result.grid(row=0, column=2, padx=10)

Window.mainloop()
